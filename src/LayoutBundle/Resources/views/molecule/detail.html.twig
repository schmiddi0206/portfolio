{% if raw is not defined or not raw %}<div style="width: 100%;">{% endif %}
    <div class="detail">
        <figure class="description-visible">
            <img src="{{ asset("bundles/layout/img/portfolio/deep.gif") }}" alt="" />
        </figure>

        <div class="description description-visible">
            <h3>Project</h3>
            <p>
                Describe your image here. Click me and then “Organize Images” to change my text.
            </p>
            {% include '@Layout/atom/sharing.html.twig' with {'raw': true} %}
        </div>
    </div>
{% if raw is not defined or not raw %}</div>{% endif %}
